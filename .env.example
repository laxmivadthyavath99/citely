# Copy to .env.local for local overrides, or set in Vercel's dashboard for deployment.
# Leave unset locally — api.js defaults to http://localhost:8000

# VITE_API_BASE=https://your-backend.onrender.com