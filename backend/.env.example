# Copy this file to .env and fill in as needed.
# Local dev works with zero config (SQLite by default in later phases).

# DATABASE_URL=postgresql://user:password@host:5432/citely   # used from Phase 2 onward
